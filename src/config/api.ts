import Config from 'react-native-config';

const publicEndpoints = [
  Config.FORGOT_PASSWORD,
  Config.LOGIN,
  Config.RESET_PASSWORD,
  Config.LOGOUT,
  Config.REFRESH_TOKEN,
  Config.RESET_PASSWORD,
  Config.SIGNUP,
  Config.VALIDATE_RESET_PASSWORD,
];

export {publicEndpoints};
